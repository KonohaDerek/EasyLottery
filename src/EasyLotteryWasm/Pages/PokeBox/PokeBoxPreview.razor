@page "/pokebox/preview/{Id:int}"
@using EasyLotteryDomain.Models.Entities
@inject PokeTemplateApiClient PokeService
@inject ObsSessionService ObsSessionService
@inject NavigationManager NavigationManager

<PageTitle>戳戳樂 OBS 預覽</PageTitle>
<p class="p-4 text-muted">正在開啟共用 OBS 預覽…</p>

@code {
    [Parameter] public int Id { get; set; }

    protected override async Task OnInitializedAsync()
    {
        var template = await PokeService.LoadTemplateAsync(Id);
        if (template is null || template.PublicId == Guid.Empty)
        {
            NavigationManager.NavigateTo("/pokebox", replace: true);
            return;
        }

        var token = await ObsSessionService.CreateObsTokenAsync("pokebox", template.PublicId.ToString(), allowControl: true);
        NavigationManager.NavigateTo($"/obs/pokebox/{template.PublicId:D}?controls=1#sessionToken={Uri.EscapeDataString(token)}", replace: true);
    }
}
