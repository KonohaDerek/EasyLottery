@page "/system/access"

<PageTitle>管理員 Passkey 狀態</PageTitle>
<Card>
    <CardHeader><CardTitle>管理員 Passkey 狀態</CardTitle></CardHeader>
    <CardBody>
        <CardText>
            管理頁只接受設定的 admin email 與 Passkey 登入。Passkey 驗證成功後，API
            會簽發短期 JWT，並透過 <code>X-EasyLottery-Session-Token</code> header 驗證管理操作。
        </CardText>
        <CardText class="text-muted">
            第一次使用請回到登入畫面註冊 Passkey。Passkey credential 只保存公鑰與簽章計數，
            私鑰會留在瀏覽器、裝置或密碼管理器。
        </CardText>
        <CardText class="text-muted">
            OBS URL 仍使用資源限定的 scoped token，不會把 admin JWT 放進 URL。
        </CardText>
    </CardBody>
</Card>
